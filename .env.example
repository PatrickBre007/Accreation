# Frontend (Vite)
# Di default il frontend chiama `/api` (stessa origin) e il server legge i prodotti da Sanity.
# Se in produzione frontend e backend sono su domini diversi, imposta il base URL dell'API:
# VITE_API_BASE_URL=https://api.tuodominio.it

# Stripe (frontend) — serve solo se usi Stripe.js/Elements nel browser.
# Con l'attuale flow (redirect a Stripe Checkout via URL dal backend) NON è necessaria.
#VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Contatti
VITE_INSTAGRAM_URL=https://instagram.com/tuo_profilo
VITE_WHATSAPP_NUMBER=39XXXXXXXXXX

# Backend (Express)
PORT=8787
FRONTEND_URL=http://localhost:5173
STRIPE_SECRET_KEY=

# Sanity (server-side)
SANITY_PROJECT_ID=0t2rybew
SANITY_DATASET=production
SANITY_API_VERSION=2025-01-01
# Se dataset privato:
SANITY_READ_TOKEN=
