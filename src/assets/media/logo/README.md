# Logo assets

Metti qui i file del logo (es. `logo.svg`, `logo-dark.svg`, `favicon.png`).

Esempio import (Vite/React):

```ts
import logo from '../assets/media/logo/logo.svg'
```

Suggerimento:
- Preferisci `SVG` per loghi (scalano meglio).
- Usa `PNG` solo se serve un effetto raster/texture.
